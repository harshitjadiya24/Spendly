description: This command seeds the database with a default user for testing and development purposes. It creates a user with predefined credentials and roles, allowing developers to quickly set up a working environment without manually creating users.

